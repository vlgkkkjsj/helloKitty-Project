name: hello_kitty_project
description: Um app fofo da Hello Kitty 💖
version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2     
  http: ^0.13.6   # Versão exigida pelo flutter_localizations e device_preview
  device_preview: ^1.1.0
  flutter_lints: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/hello_kitty.gif